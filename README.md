# Password Generator (Java)

A simple console-based password generator that allows the user to:
- Choose password length.
- Include or exclude numbers and special characters.
- Ensures the first character is always a letter.

Technologies: Java, Scanner, Random
